version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gocritic
    - gosec
    - misspell
    - nilerr
    - noctx
    - sqlclosecheck
    - unconvert
    - unparam
    - whitespace

run:
  timeout: 5m
